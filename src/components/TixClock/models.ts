export interface ITixClock {
  'column-1': {
    color: string
    leds: boolean[]
  }
  'column-2': {
    color: string
    leds: boolean[]
  }
  'column-3': {
    color: string
    leds: boolean[]
  }
  'column-4': {
    color: string
    leds: boolean[]
  }
}

export interface IColumn {
  columnName: string
}
